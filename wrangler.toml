  name = "raga-ray"                  # اسم Workerت (یونیک باشه، می‌تونی عوض کنی)
    compatibility_date = "2026-02-16"     # تاریخ امروز یا جدیدتر
    compatibility_flags = ["nodejs_compat"]  # اگر node APIs مثل fs یا crypto نیاز داری
    
    main = "src/index.ts"                 # مسیر فایل اصلی Worker (اگر بعد از tsc بشه dist/index.js، عوض کن به "dist/index.js")
    
    # اختیاری: برای امنیت و env vars
    [vars]
    # BOT_TOKEN = "..."   ← بهتره از Secrets پنل Cloudflare استفاده کنی نه اینجا

  # اگر بعداً KV یا D1 اضافه کردی، اینجا بایندینگ تعریف کن